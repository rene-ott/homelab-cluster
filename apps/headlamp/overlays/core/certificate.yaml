---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: apps-wildcard
  namespace: headlamp
spec:
  secretName: apps-wildcard-tls
  issuerRef:
    name: ${cert_issuer}
    kind: ClusterIssuer
  dnsNames:
    - "*.${domain_apps}"
